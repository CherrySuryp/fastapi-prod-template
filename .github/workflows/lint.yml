name: "Linters"
on:
  push:
    branches: [master]
  pull_request:
    branches: [master]
jobs:
  ruff_lint:
    runs-on: ubuntu-latest
    steps:
      - uses: astral-sh/ruff-action@v3
        with:
          src: .
